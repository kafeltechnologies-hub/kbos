<div class="min-h-screen bg-slate-100 p-6 text-slate-900">
@include('livewire.finance._header', ['title'=>'Bank Reconciliation','subtitle'=>'Match bank transactions with ledger entries.'])
@include('livewire.finance._nav')
<div class="bg-white border border-slate-300 p-4 mb-6"><input wire:model.live.debounce.500ms="search" placeholder="Search reference, account or narration..." class="w-full border px-3 py-2 text-xs"></div>
<div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6"><div class="bg-white border p-5"><p class="text-[10px] uppercase font-bold text-slate-500">Debit</p><p class="text-2xl font-black font-mono">{{ number_format($totalDebit,2) }}</p></div><div class="bg-white border p-5"><p class="text-[10px] uppercase font-bold text-slate-500">Credit</p><p class="text-2xl font-black font-mono">{{ number_format($totalCredit,2) }}</p></div><div class="bg-white border p-5"><p class="text-[10px] uppercase font-bold text-slate-500">Balance</p><p class="text-2xl font-black font-mono">{{ number_format($totalDebit-$totalCredit,2) }}</p></div></div>
<div class="bg-white border border-slate-300 overflow-x-auto"><table class="w-full text-xs"><thead class="bg-slate-100"><tr><th class="p-3 text-left">Date</th><th class="p-3 text-left">Account</th><th class="p-3 text-left">Reference</th><th class="p-3 text-left">Narration</th><th class="p-3 text-right">Debit</th><th class="p-3 text-right">Credit</th></tr></thead><tbody class="divide-y">@forelse($entries as $entry)<tr><td class="p-3">{{ $entry->entry_date?->format('d M Y') ?? $entry->transaction_date?->format('d M Y') }}</td><td class="p-3">{{ $entry->account_name ?? $entry->account }}</td><td class="p-3 font-mono">{{ $entry->reference }}</td><td class="p-3">{{ $entry->description ?? $entry->narration }}</td><td class="p-3 text-right font-mono">{{ number_format((float)($entry->debit ?? $entry->debit_amount ?? 0),2) }}</td><td class="p-3 text-right font-mono">{{ number_format((float)($entry->credit ?? $entry->credit_amount ?? 0),2) }}</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-slate-400 font-bold">No entries found.</td></tr>@endforelse</tbody></table></div>
</div>
